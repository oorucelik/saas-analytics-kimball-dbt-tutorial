name: 'dbt_ravenstack'
version: '1.0.0'

profile: 'dbt_ravenstack'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  dbt_ravenstack:
    +schema: raw

models:
  dbt_ravenstack:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
